# 用户使用手册

完整版本请查看：

- [docs/用户使用手册.md](./docs/用户使用手册.md)

---

## 快速使用（必备软件）

在本地使用本项目前，请先安装：

1. Node.js（建议 LTS 版本 18 或 20+）
2. npm（安装 Node.js 后自动包含）
3. 现代浏览器（Chrome / Edge / Firefox）

可选：

- Git（用于拉取和更新项目）
- VS Code / Cursor（用于维护项目）

版本检查：

```bash
node -v
npm -v
```

---

## 快速启动

```bash
npm install
npm run dev:server
npm run dev
```

默认访问地址通常为：

`http://localhost:5173`

其中 API 默认运行在：

`http://localhost:3000`

